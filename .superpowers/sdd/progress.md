# SDD Ledger — feat/rig-reconciler

Plan: kb/Carla/exec-plans/active/feat-rig-reconciler/plan.md (16 tasks). Spec: kb/Carla/specs/drafts/bulletproof-rig-session-round-trip-*.md
Baseline: 533 tests green at 5f2952932 (empty divergence commit over main 9ff9921-lineage).
INVOCATION NOTE: run tests as `uv run python -m pytest` — bare `uv run pytest` breaks: source/mcp shadows the mcp package under the console-script entrypoint.
Companion Rust plan: COMPLETE, looperdooper feat/rig-managed-mode @ 1466448 pushed, MERGE-READY (unmerged; contract live-verified).
Model policy: sonnet implementer+reviewer for transcription tasks; opus for Tasks 9, 10, 14 (converge load/save, bridge integration) + final review fable.
Briefs: .superpowers/sdd/rig/task-N-brief.md + global-constraints.md + watchpoints.md

## Progress
- Task 1 BASE: 5f2952932
- Task 1: complete (commit 44cebb064, review clean — graph v3 model exact. MINOR for final review: remove_edge still keys on (src,dst) only, silently removes first of multiple port-level edges. Report miscounted suite 528 vs real 547 — verified green)
- Task 2 BASE: 44cebb064
- Task 2: complete (commit b024fbf28, review clean — ObservedState model, loop discovery, 556 green)
- Task 3 BASE: b024fbf28
- Task 3: complete (commit f7bd6a760, review clean — port expansion byte-identical to controller rules; 562 green. MINORS: unused imports Optional/Link/ObservedState (brief-verbatim, staged for Task 4); out:main capped [:2])
- Task 4 BASE: f7bd6a760
- Task 4: complete (commit 29b4a677a, review clean incl adversarial probes — diff engine pure, no double-counting, reversed-link semantics correct; 573 green)
- Task 5 BASE: 29b4a677a
- Task 5: complete (commit d557791be, review clean — Action/plan/render_report verbatim, no-mutation verified; 581 green)
- Task 6 BASE: d557791be
- Task 6: complete (commit 60c52f179, review clean — manifest v3 exact, 590 green. MINOR for final review: session_from_dict raises bare KeyError not SessionError on missing required keys (brief-verbatim); Task 7 to confirm legacy detection via rig_manifest.json presence is sufficient)
- Task 7 BASE: 60c52f179
- Task 7: complete (commit 9772c32ed, review clean — migrator lifts all 5 real sessions read-only (8/6, 2/0, 0/0, 10/13, 7/5 nodes/edges), ordering-dependency + colon-name checks passed. USER AWARENESS: quartet-autoeq-wip + quartet-build reference looper session dirs missing on disk (pre-existing, named in notes))
- Task 8 BASE: 9772c32ed
- Task 8: complete (commit cb0771ae9, review clean — RigOps 14 methods, reset/verify, staged imports accounted exactly to Tasks 9-10; 603 green)
- Task 9 BASE: cb0771ae9
- Task 9: complete (commit 03439fa16, opus adversarial review clean — do_load sequence exact, retry pass disconnects unexpected links (neutralizes retro's scramble bug), idempotent re-load. MINORS: import_rig_state success flag ignored (messages only); no pre-flight verify_session_files; unused name param (brief-mandated). LIVE RISK flagged: single-retry vs PipeWire re-restoration timing untested at integration level)
- Task 10 BASE: 03439fa16
- Task 10: complete (commits 82df51609+80e3b23ca — do_save + self-verify + link lifting; opus review found IMPORTANT silent-drop of port_index-less loopers (brief bug vs global constraint), fixed with named notes + mixed-case regression test, re-review APPROVED. MINOR: F541 no-interp f-string. Round-trip seam loop:N<->loopers:loopN_out verified. Full suite now 633 (both test dirs))
- Task 11 BASE: 80e3b23ca
- Task 11: complete (commit 5617aaea7, review clean — do_stop reverse teardown; PLAN BUG caught+fixed: brief's negated-start-rank sort key put a2j 3rd, contradicting brief's own prose+test; local UNIT_STOP_ORDER dict used instead, adjudicated correct)
- Task 12 BASE: 5617aaea7
- Task 12: complete (commit 191696afd, review clean — observe() shielded probes, snapshot_handles pure append to shipped controller, rig_handles registered without reorder, all 4 count literals bumped incl 2 the brief missed)
- Task 13 BASE: 191696afd
- Task 13: complete (commit 59a8e23a5, review clean — 3 looper MCP tools, wire keys verified byte-for-byte against Rust api.rs on companion branch)
- Task 14 BASE: 59a8e23a5
- Task 14: complete (commit c5f26038e, opus adversarial review clean — BridgeOps full RigOps, GetState envelope unwrap caught+pinned (brief would have shipped broken), _stop_looper double-call safe, no stale-graph path. MINORS for final review: (1) start_unit "exited" heuristic drops non-exited failure diagnostics from Notes (spec-inherited; verdict still degrades downstream); (2) do_save's direct looper_get_state can propagate JSONDecodeError on half-open socket — violates never-raise contract, 3-line catch would fix. SSE cross-process arg shapes unverified end-to-end)
- Task 15 BASE: c5f26038e
- Task 15: complete (commits e1179c44b + 3f9f97fb2 — round-trip integration passed first try; review found tests couldn't distinguish clean-slate from retry-disconnect; added ordering-pin test with negative control (FAILS without clean-slate block: disconnect@7 vs first connect@0). 652 green)
- Task 16 BASE: 3f9f97fb2
- Task 16: complete (kb f04c150 + outer 8fc14f35a — 3 doc edits, backfill item left unticked. Obsidian workspace noise reverted in kb)
- ALL 16 TASKS DONE. Final whole-branch review (fable) next. MERGE_BASE: 5f2952932
- FINAL REVIEW (fable): MERGE-READY, one fix-before-merge (JSONDecodeError leak) fixed in 816b35f9d + re-review pending; IMPORTANT follow-up recommended post-merge: diff() should compare live mute state vs app:looper (verify_rig currently blind to mute drift). Minor triage: all leave. Live-integration risk register in final review (top: merge Rust branch FIRST — bridge hard-requires --managed binary).
- Branch pushed. 655 tests green.
- Fix re-review APPROVED. FINAL VERDICT: MERGE-READY unconditional (this repo). Sequencing: Rust branch must merge+build FIRST (bridge unconditionally passes --managed). PYTHON PLAN COMPLETE at 816b35f9d, awaiting user merge decision.

## feat/bridge-owned-rig — phase 1 (plan: kb/Carla/exec-plans/active/feat-bridge-owned-rig/plan.md)
- Started 2026-09-14. MERGE_BASE: b135b4cef (branch tip before Task 1). Spec review PR #3 still open; branch push is hook-blocked until approved (local commits only).
- Live/process steps (Task 7 manual GUI + headless checks, Task 18 ./dev live) deferred to controller at the end, after confirming no rig is running.
- Task 1 review: spec ✅; Important x2 → (a) laziness test fixed via fix dispatch, (b) trailer finding resolved by amending the plan: trailers name the model that wrote the commit (no history rewrite). Minor (leave for final triage): config.py uses Optional[...] rather than X | None.
- Task 1: complete (commits b135b4c..c353bf1, review clean after 1 fix). Task 2 BASE: c353bf18d
- Task 2: complete (commits c353bf1..d330649, review clean). Minor (final triage): backends/pw_link.py typing.List/Optional vs builtins; redundant list() wraps at pw_link.py:70,74; test doesn't cover TimeoutExpired / non-zero returncode branches. Task 3 BASE: d3306493a
- Task 3 review: spec ✅; Important: 5 failure branches untested (connect/read timeout, empty reply, id mismatch, non-object) → fix dispatched (tests only). Minor (final triage): rpc.py RpcError.__init__ shadows builtin `type`; writer.close() without await wait_closed().
- Task 3: complete (commits d330649..7c2f473, review clean after 1 fix; suite 673). Task 4 BASE: 7c2f473c5
- Task 4 review: spec ✅, callback mapping verified against carla_host.engineCallback. Important: events test leaks global cache on assertion failure → fix dispatched (try/finally). ⚠️ trailer resolved by controller: Task 4 implementer was dispatched on haiku, trailer "Claude Haiku 4.5" is accurate. Minor (final triage): malformed CONNECTION_ADDED parsing untested; duplicate CLIENT_ADDED for existing group drops its ports (unconfirmed vs real engine).
- Task 4: complete (commits 7c2f473..3ec9647, review clean after 1 fix). Task 5 BASE: 3ec9647e2
- Task 5 review: spec ✅ (allowlist = 20 names; host names/arg orders/dict keys verified). Important x2 → fix dispatched: (a) add_plugin count race — controller resolved ⚠️: Task 6 ThreadingTCPServer does NOT serialize, so race is real → lock in dispatch; (b) non-id arg type errors untyped — controller resolved ⚠️: Task 6 boundary maps bare exceptions to `internal`, fix makes them `validation`. Minor x3 (final triage): see task-5-review.md.
  - Task 5 Minor detail: (1) custom_data_get/set always use Carla custom-data type "string" regardless of key (design note); (2) dispatch getattr safety relies on ALLOWLIST matching method names — enforced by test_every_allowlisted_method_exists_on_api; (3) per-call count lookups add host round-trips — negligible, reviewer says leave.
- Task 5 fix 1 (a8cad57): dispatch lock + _int/_float validation. Re-review 1: code correct; Important = validation test gaps (param_id, patchbay_connect args 2-4, connection_id). Fix 2 dispatched (tests only). Accepted deviation: 1ms sleep in fake get_current_plugin_count to make the race reproducible under the GIL.
- Task 5: complete (commits 3ec9647..ad9f715, review clean after 2 fixes). Minor (final triage): 4 new tests in test_worker_api.py:222,244,257,274 carry docstrings unlike the rest of the file.
- Task 6 BASE: ad9f7159a
- Task 6 implemented (99365cf, haiku; 4 tests, suite 707). Review dispatched (sonnet) → task-6-review.md.
- Task 6: complete (commits ad9f715..99365cf, review clean). Minor (final triage): (1) server.py:145 `req.get("params") or {}` coerces falsy non-dict params ([]) to {} instead of validation; (2) server.py:181-185 self._thread stored but never joined (harmless); (3) server.py:19 module-level logger vs "no module-level mutable state" wording (brief-mandated; logger handle not business state).
- Task 7 BASE: 99365cfef
- Task 7 implemented (642f56a, sonnet; 3 tests, suite 710; manual GUI/headless checks DEFERRED to controller live phase). Review dispatched (sonnet) → task-7-review.md.
- Pre-dispatch check: Task 8 brief CarlaClient signatures match worker/api.py (21 methods). Task 9 brief wire format verified against looperdooper (remote.rs GetState {state:{loopers:[id,name,port_index,mode,level_db,pan,input_source]}}, {ok:true}/{error}, Command SaveSessionAt/LoadSession/SetMainOutputMute/SetAllOutputsMute, LooperTarget Id(u32)) — no drift.
- Task 7 review: spec ✅. Important: slot_startRpcWorker doesn't guard attach() OSError (port in use) / bad CARLA_RPC_PORT ValueError → fix dispatched (sonnet), also covering controller-found related bug: attach() leaves events hook registered to orphan cache when server.start() raises. Minor (in fix): headless init-failure test doesn't assert teardown. Minor (final triage): closeEvent calls slot_stopRpcWorker before engine-stop-retry check (mirrors pre-existing MCP server behaviour). ⚠️ resolved: live GUI/headless behaviour → controller live phase (deferred); 710 suite figure from implementer report.
- Task 7 fix 1 (adcb5e8): guarded slot start (OSError/ValueError), attach() unregisters events on failed start, port-conflict test + headless teardown assertion; suite 711. Re-review dispatched (sonnet).
- Pre-dispatch check: Task 12 BridgeOps covers all 14 RigOps methods with matching signatures (wait_ports default 15.0 vs rig constant: LOAD_PORT_TIMEOUT_S = 15.0). Task 14 brief rig imports (Node, RigGraph, RuntimeUnit, Link, ObservedState, describe_rig, diff, in_rig_port_space) all exist.
- Pre-dispatch check: Task 15 FastMCP 2.14.4 supports tool(annotations=) + mcp.types.ToolAnnotations. Task 16 imports (do_load, do_save, SessionError, read_session, RigSession, write_session) exist; _SESSION_FILES defined in-brief. Task 17 OPEN: linters/ is the Reinicorn kb lint framework (discovers rules/**/*.sh via framework/run-lints.sh); plan puts linters/python_rules.py at top level, invoked only by ./dev lint — decide placement / rcorn-update overwrite risk before dispatching Task 17. test parents[4] path = repo root (correct).
- Task 7: complete (commits 99365cf..adcb5e8, review clean after 1 fix). Manual GUI/headless checks still DEFERRED to controller live phase.
- Task 8 BASE: adcb5e862
- Task 17 placement RESOLVED (controller): .reinicorn/manifest.json tracks only specific bundled linters files (README, .lint-config.json, framework/*, rules/kb/*.sh, rules/scripts/shellcheck.sh, ci-recovery/*, structural-tests/.gitkeep); a new linters/python_rules.py is not a bundled file, so rcorn update won't overwrite it and run-lints.sh (walks rules/**/*.sh) won't pick it up. Keep plan path; ./dev lint invokes it directly.
- kb progress.md updated with Tasks 1-7 + pre-dispatch checks; published (kb 757b8a9). Superproject kb pointer bump deferred to a between-task gap (avoid polluting a task's review range).
- Task 8 implemented (134b2d0, haiku; 3 tests, suite 714). Review dispatched (sonnet) → task-8-review.md.
- Task 8: complete (commits adcb5e8..134b2d0, review clean). Minor (final triage): backends/carla.py:26 unused Any/Optional imports (brief sample). ⚠️ resolved: suite count from implementer report; lint enforcement arrives in Task 17 (./dev lint over backends/).
- Task 9 BASE: 134b2d077
- Task 9 implemented (8aa884d, haiku; 5 tests, suite 719). Review dispatched (sonnet) → task-9-review.md.
- Dispatch prep: Task 10 (processes.py) standalone, consumes nothing from prior tasks; ports_present(prefix, list_outputs) takes pw_link.list_outputs() -> List[str] as a callable; ProcessManager(log_dir) ← BridgeConfig.log_dir (Path, $XDG_STATE_HOME/carla-mcp). Task 11 (bridge/result.py) maps backends.rpc.RpcError(type,message) 1:1. Both briefs have complete code → haiku; trailer override needed (briefs say Opus 5).
- Task 9: complete (commits 134b2d0..8aa884d, review spec ✅ / quality Approved). PLAN-MANDATED Important — PENDING HUMAN DECISION (asked in turn-end message; fold into final-review fix wave if approved): backends/looper.py:40-43 get_state() falls back to returning the raw reply when there is no {"state":...} envelope instead of raising RpcError("internal", ...); unreachable against current looperdooper (remote.rs always wraps). Minor (final triage): looper.py:11 unused Optional import; fallback branch untested. ⚠️ resolved: suite count from implementer report.
- Task 10 BASE: 8aa884d31
- Task 10 implemented (b7d8057, haiku; 6 tests, suite 725). Review dispatched (sonnet) → task-10-review.md.
- Pre-dispatch check: Task 13 spawn argv verified — looperdooper main.rs has --managed and --remote-json-port; /usr/bin/a2jmidid supports -e; pw-jack installed; CARLA_MCP_PORT env matches existing mcp_stdio_bridge.py:146 convention. Task 12 (475-line brief, 3 new files + ops test, consumes Tasks 8-11 + rig.observe/converge, lazy bridge.units imports) → sonnet. Task 13 (units.py replace stubs) → haiku/sonnet.
- Pre-dispatch check: Task 12 brief DOES create bridge/units.py stub (brief line 438; committed at line 471) though its Files header omits it — tell the Task 12 implementer to create and commit it. legacy_sse imports (mcp.ClientSession, mcp.client.sse.sse_client) installed.
- Task 10: complete (commits 8aa884d..b7d8057, review spec ✅ / quality Approved). PLAN-MANDATED Important — PENDING HUMAN DECISION (batched with Task 9 question): backends/processes.py:36-43 spawn() doesn't guard an already-running unit name → old process orphaned, old log fd leaked. Minor (final triage): processes.py:69-79 stop() typed -> str|None but always returns None. ⚠️ resolved: log_dir/list_outputs wiring is Tasks 12-13; python_rules lints only except/skip rules, not typing.
- Task 11 BASE: b7d805766
- Task 10 pending-decision context: Task 13 callers guard spawn with probes (tcp_reachable 8089 / ports_present loopers: / a2j_running), NOT is_running — so a second start after a readiness timeout (process alive but not yet reachable) would respawn carla:main/looper:engine and orphan the first. Real scenario; recommend guard in spawn() (return existing pid when is_running).
- Task 11 implemented (9cc21f6, haiku; 5 tests, suite 730). Review dispatched (sonnet) → task-11-review.md.
- Pre-dispatch check: Task 12 BridgeOps.observe calls rig.observe.observe with matching kwargs (list_links/list_outputs/list_inputs/unit_probe/looper_get_state/carla_handles); carla_handles comes from legacy SSE rig_handles (rig/register.py:305 → {nodes:{node:{plugin_id:handle}}}), matching observe's Dict[str,Dict[str,str]] — NOT CarlaClient.handles() (dict[str,int]); no mismatch. BridgeConfig fields used (carla_rpc_port, carla_sse_url, looper_port) exist.
- Task 11: complete (commits b7d8057..9cc21f6, review clean). Minor (final triage, plan-mandated): result.py:3-5 docstring says "three" allowed except-Exception sites (constraints name four); result.py:33-35 fail() doesn't validate type against ERROR_TYPES like ToolError does.
- Task 12 BASE: 9cc21f679
- Pre-dispatch check: Task 15 — server.main() = Bridge.from_env() + FastMCP stdio run; from_env builds lazy per-request transports (no connect), ProcessManager(log_dir), _git_rev; no spawn/connect at startup → the '< /dev/null' smoke is safe to let the implementer run. Bridge.version + session_name exist in Task 12 brief; worker version() returns {'worker': ...} matching rig_state's carla.version()['worker']. tests/data/ does not exist yet (Task 15 creates it). __pycache__ gitignored, so 'git add bridge/' is safe. Task 14 (pure state_view, complete code, 2 files) → haiku.
- Task 12 implemented (df51a80, sonnet; 5 tests, suite 735; units.py stub included). Review dispatched (opus — destructive-ops integration adapter) → task-12-review.md.
- Pre-dispatch check: Task 13 — committed units.py stub signatures match brief (start/stop_carla_main + start/stop_looper_engine async; start/stop_a2j sync), and ops.py awaits exactly the async ones (ops.py:75-97). Brief constants CARLA_READY_TIMEOUT_S=20.0, LOOPER_READY_TIMEOUT_S=10.0, POLL_S=0.5. UNIT_START_ORDER defined at rig/reconcile.py:252 (Task 15 reference valid). Task 13 (1 module replace + test, complete code) → haiku.
- Task 12: complete (commits 9cc21f6..df51a80, review opus: spec ✅ / quality Approved). PLAN-MANDATED Important x3 — PENDING HUMAN DECISION (batched; reviewer says fix before phase 1 drives the live rig):
  (12a) legacy_sse.py:27-31 call_tool ignores CallToolResult.isError → failed import_rig_state gives empty messages, export_rig_state None → do_save "carla not reachable", remove_node None → do_stop reports "Stopped".
  (12b) legacy_sse.py:21-24 no overall timeout on SSE call (15s pings reset 300s read timeout) → a wedged legacy tool hangs the bridge; observe() calls rig_handles every time.
  (12c) app.py:36-37 5s JsonLinesTransport default also applies to project_load/save and looper LoadSession/SaveSessionAt → slow load reported failed while still loading; do_load proceeds against half-loaded project.
  Minor (final triage, all plan-mandated): ops.py:114-122 wait_ports time.sleep blocks loop + unit_probe subprocesses on loop; app.py:47,55-56 for_tests clients point at LIVE 8089/8088 (safety — recommend fixing with the batch); app.py:46 for_tests leaks temp dirs; app.py:62-69 _git_rev misses PermissionError/OSError; legacy_sse.py:26 anyio ExceptionGroup message; missing tests (call_tool, carla-child remove, import/export RpcError, looper session paths); unused imports/mocks (app.py:5, test 2,14,91-92); ops.py:87-88 carla-child stop while carla down reports Stopped.
  ⚠️ → live phase: real .carxp/looper session load durations; live isError/output-schema behaviour; host.load_project from worker socket thread (Task 7 threading; precedent: legacy in-GUI MCP thread also called host API).
- Task 13 BASE: df51a807f
- Pre-dispatch check: Task 16 — Task 15 placeholder 'async def load_session_into(b: Bridge, name: str) -> List[str]' (brief-15 line 350) matches Task 16's replacement signature (brief-16 line 135); Task 16 modifies tests/data/tool_surface.json that Task 15 creates (adds session_save/session_load/session_list). Consistent.
- Pre-dispatch check: Task 17 dev edits — brief's replacement mcp) block is byte-identical to current dev lines 38-41 (no-op); bridge/worker/lint/live cases are additive; run/build/test/help untouched; usage text must list the 4 new commands; .mcp.json left alone. ./dev live targets tests/live (created by Task 18).
- Pre-dispatch check: Task 17 lint dry-scan — no bare except, no except Exception/BaseException outside the 4 allowlisted files, and no reasonless pytest skip in bridge/ backends/ worker/ as of df51a80 → ./dev lint should report 0 findings on current code (re-check after Tasks 13-16 land).
- USER DECISION (2026-09-14): "yes to everything but no need to preserve any legacy crap if its not needed" → APPROVED: (9) get_state raises on missing envelope; (10) spawn() returns existing pid when running; (12a+b) legacy SSE isError + overall timeout; (12c) longer timeouts for project/session load/save; (for_tests) unreachable ports. PLUS: remove legacy compatibility that isn't needed rather than preserving/hardening it. Controller assessing which legacy paths are load-bearing before dispatching the fix batch (after Task 13 review, before Task 14).
- Task 13 implemented (866bd42, haiku; 6 tests, suite 741). Review dispatched (sonnet) → task-13-review.md.
- Legacy assessment: KEEP legacy SSE shim for phase 1 (only route to rig/controller.py ~1262 lines: rig_handles, remove_node, import/export_rig_state; dies in phase 2) — harden minimally. KEEP legacy rig_manifest.json reading (4 real sessions in ~/.config/rig-sessions are manifest-only: full-rig-6loops-2026-04-03, quartet-autoeq-wip, quartet-build, uke-bass-clean-2026-04-10). REMOVE LooperClient.get_state bare-reply fallback.
- FIX BATCH 1 dispatched (sonnet, parallel with Task 13 read-only review), BASE 866bd4208, report fixbatch-1-report.md, 5 commits: (1) get_state envelope raises; (2) spawn() guard returns existing pid; (3) legacy_sse isError→RpcError, 30s overall deadline, ExceptionGroup unwrap, call_tool tests, ops legacy call sites return error strings (remove_node failure / carla-child stop while carla down); (4) per-call timeout override, LONG_OP_TIMEOUT_S=120 for project_load/save + looper load_session/save_session_at; (5) for_tests unreachable ports + _git_rev OSError. If Task 13 review needs a fix, dispatch it AFTER batch 1 commits.
- Later-brief impact of fix batch 1: Tasks 15/16 tests use Bridge.for_tests() with AsyncMock'd clients — unreachable-port change is transparent (unmocked calls now fail fast with backend_unavailable instead of reaching live rig). No brief uses get_state fallback, CarlaRpc.call timeout, or spawn respawn. kb decisions.md: added #13 (approved hardening + decision-12 correction: shim carries 4 verbs incl. remove_node) and #14 (legacy policy); publish with next kb update.
- Task 13: complete (commits df51a80..866bd42, review clean). Minor (final triage, plan-mandated): units.py:48-49,66-67,80-81 stop_* report success for adopted external instances (ProcessManager.stop returns None for never-spawned names — can't distinguish "stopped" vs "wasn't ours"); units.py:36 env.setdefault("CARLA_MCP_PORT") lets a stale inherited CARLA_MCP_PORT override config.
- Task 14 BASE: set after fix batch 1 commits (dispatch Task 14 only once batch 1 is DONE, to keep commit ranges separate).
- FIX BATCH 1 DONE (sonnet): 6c8a682 get_state envelope; ddf37a9 spawn guard; 74f8da1 legacy_sse isError+30s deadline; 9aedab3 per-call LONG_OP_TIMEOUT_S=120; a55ed98 for_tests closed ports 1 (carla) / 2 (looper) + _git_rev OSError. Suite 760. Review dispatched (opus) → fixbatch-1-review.md (range 866bd42..a55ed98). Any batch-review fix goes AFTER Task 14 commits.
- Task 14 BASE: a55ed9800 (dispatched haiku in parallel with read-only batch review).
- Task 14 implemented (65bb83e, haiku; 5 tests, suite 765). Review dispatched (sonnet) → task-14-review.md.
- Task 14 review: spec ✅ (matches brief) / quality Approved, BUT Important plan-mandated: state_view.py:46 forwards RigDiff.issues() incl. unexpected connections → DEGRADED, contradicting the approved SPEC (spec lines 175-178: "Hand edits are notes, absorbed on save … appear in rig_state as notes, not as DEGRADED"; user decision 9 in brainstorming). Controller resolved ⚠️ from spec text: spec governs over plan code; user already decided → fix dispatched (sonnet) in state_view.py only (reconcile.py untouched: RigDiff.issues/verdict still used by converge verify/report + legacy server). Also adds Minor: plain-string focus test. Runs in parallel with read-only batch-1 review (no file overlap).
- Task 15 compat with Task 14 fix: brief's test_rig_state_compare_reads_named_session gets DEGRADED from a down unit (carla:main probe False), not an unexpected link → unaffected by notes-not-DEGRADED fix. rig_up test only checks 'verdict' key presence.
- Task 14 fix 1 (70c10ff, sonnet): unexpected rig-space links → notes; verdict from filtered issues; +3 tests (note-not-DEGRADED fail-before shown, missing edge degrades, plain-string focus); unresolved_effects kept as issue per spec list. Suite 768. Re-review dispatched (sonnet).
- FIX BATCH 1 REVIEW (opus): all 5 fixes ✅, verdict Approved. Deadline verified against a real mcp 1.26 SSE server on ephemeral ports (hung connect/handshake/tool all bounded, clean cancel, outer CancelledError propagates). Follow-up Important (controller-chosen value, not user-approved number): 30s too short for import_rig_state on quartet-build (~15-40s) → follow-up fix dispatched (sonnet): per-tool deadline, 120s import/export, 30s handles/remove_node; + Minor BaseExceptionGroup must not swallow KeyboardInterrupt/SystemExit; + Minor export failure no longer reported as "carla not reachable". Minor (final triage): ops.py:56-57 rig_handles failure indistinguishable from handle drift; test_bridge2_app.py:28-30 env-override test doesn't assert ports; test_bridge2_app.py:20-25 refuse-fast test passes on old code / no timing; test_backends_legacy_sse.py:42-54 fake SSE lacks task group (real cancel path covered only by reviewer experiment).
  NOTE: reviewer's fail-before check made 2 bare TCP connects to 127.0.0.1:8089/:8088 — both refused (nothing listening at that time), no payload sent. Tell user.
  ⚠️ → live phase: real durations (quartet import, carla project load/save, looper LoadSession/SaveSessionAt); legacy server continuing import after client disconnect at deadline.
- ⚠️ resolved (controller, looperdooper source): looper remote.rs:332-335 replies ok_response() immediately after enqueuing a Command — LoadSession runs on engine worker thread (engine_commands.rs:689), SaveSessionAt via session_saver. So looper LONG_OP_TIMEOUT_S is harmless but moot (reply is instant); completion is NOT signalled by the reply. do_load already waits via wait_ports (converge.py:227). Checking do_save for a verify race after async save.
- Task 14: complete (commits a55ed98..70c10ff, review clean after 1 fix; hand edits = notes per spec). Minor (final triage): state_view.py:47-53 filters issues by rendered string prefix "unexpected connection: " instead of by the RigDiff.unexpected_connections list — breaks silently if reconcile.py wording changes.
- Task 15 BASE: set after legacy SSE follow-up fix commits.
- LATENT BUG found (pre-existing, inherited from mcp_stdio_bridge.py:677): do_save (converge.py:353) sends looper SaveSessionAt, then soon runs verify_session_files (session.py: checks looper/project.loopers exists). Looper replies ok on enqueue (remote.rs:334) and saves in background (>100 MB per decision 6) → first save can report false DEGRADED "missing file: looper/project.loopers"; re-save can verify a STALE project.loopers as OK. Plan: after legacy SSE follow-up commits (ops.py overlap), dispatch fix in BridgeOps.looper_save_session_at to wait for save completion (predicate from looperdooper session.rs write pattern) bounded by LONG_OP_TIMEOUT_S, returning an error string on timeout. Not a plan contradiction (addition inside BridgeOps). Tell user.
- Save-race fix DESIGN (from looperdooper session.rs:212-280): saver serializes every looper's audio first (bounded by LOOPER_SAVE_TIMEOUT), THEN File::create("project.loopers") + one writeln! of the full pretty JSON containing "save_time" (timestamp_millis); SaveSessionAt uses save_exact → writes exactly into the given dir. Completion predicate for BridgeOps.looper_save_session_at: record request_ms before sending; poll <dir>/project.loopers until it parses as JSON AND save_time >= request_ms - 2000 (clock-skew slack); partial write → parse fails → keep polling; stale file → older save_time → keep polling; bound by a timeout > LOOPER_SAVE_TIMEOUT, else return error string "looper save did not complete within Ns". Poll with asyncio.sleep (async method). Dispatch after legacy SSE follow-up commits.
- Save-race fix sizing: looperdooper LOOPER_SAVE_TIMEOUT = 10s (session.rs:70, cumulative budget for all loopers to serialize before project.loopers is written) → bridge poll bound = LONG_OP_TIMEOUT_S (120s), comfortably above it plus disk flush of >100 MB. Order: legacy SSE follow-up commits → save-race fix + review → Task 15 (no concurrent commits).
- Legacy SSE follow-up DONE (0f80d05, sonnet; suite 774): call_tool(timeout=), LEGACY_SSE_LONG_TIMEOUT_S=120 for import/export; BaseExceptionGroup re-raises non-Exception members; export errors passed through via {"error":...} dict + do_save three-way branch in rig/converge.py (shared module — review checks legacy stdio path). Review dispatched (sonnet) → fixbatch-1-followup-review.md (range 70c10ff..0f80d05).
- Save-race fix DISPATCHED (sonnet, BASE 0f80d0594) → savefix-report.md; runs in parallel with read-only follow-up review. Any follow-up-review fix waits for savefix to commit (ops.py overlap).
- Save-race fix DONE (4c88736, sonnet; +4 tests in test_bridge2_ops_save.py, stale-file test fail-before shown; suite 778). Review dispatched (sonnet) → savefix-review.md (range 0f80d05..4c88736).
- Task 16 compat with save fix: brief's session tests patch do_save/do_load (brief-16 lines 63, 88), so BridgeOps.looper_save_session_at's 120s poll is never hit in unit tests. Live Task 18 will exercise it for real.
- Legacy SSE follow-up REVIEW: (1) ✅ (2) ✅ (3) ✅, converge.py three-way branch verified against all do_save callers + export_rig_state impls (legacy stdio path byte-identical), verdict Approved, no issues. ⚠️ resolved: "uncommitted WIP in ops.py" seen by reviewer was the in-flight save fix, since committed as 4c88736 (tree clean). FIX BATCH 1 + follow-up: COMPLETE (866bd42..0f80d05).
- Task 15 BASE: 4c88736a2 (dispatched sonnet in parallel with read-only savefix review; any savefix-review fix waits for Task 15 commit).
- Pre-dispatch re-check: Task 16 imports resolve (from carla_mcp.rig.converge import LOOPER_PROJECT, do_load, do_save — OK; LOOPER_PROJECT='project.loopers' defined rig/session.py:29). Task 17 lint dry-scan re-run at 4c88736: still 0 would-be findings in bridge/ backends/ worker/ (no bare except, no broad except outside allowlist) and no reasonless pytest skips in tests/.
- Save-race fix REVIEW: correctness ✅ (save_time i64 epoch-millis stamped at start of execute_save_session, only writer of project.loopers; 2s slack safe; partial writes rejected), quality Approved. Minor (final triage): bridge/ops.py:41 UnicodeDecodeError caught only implicitly via ValueError ancestry. ⚠️ resolved by reviewer trace: SessionSaver queue delay can only push save_time later, never cause false timeout. SAVE FIX: COMPLETE (0f80d05..4c88736).
- Task 15 implemented (4197b1a, sonnet; 9 tests, suite 787; smoke 'python -m carla_mcp.bridge < /dev/null' exit 0, empty stdout; sessions.py placeholder + tests/data/tool_surface.json created). Review dispatched (sonnet) → task-15-review.md.
- Pre-dispatch check: Task 16 — committed sessions.py placeholder has load_session_into (NotImplementedError) + build()->[]; register_all already iterates (lifecycle, sessions) so Task 16 needs no registry edit. Brief's 3 snapshot entries match committed tool_surface.json format ({params:[...], annotations:{...}}); implementer must add a comma after rig_reset_routing entry. Task 16 → sonnet (replaces module + json + test, uses do_load/do_save/BridgeOps).
- Task 15 review: spec ✅ / quality Approved. Important (plan-mandated, internal plan inconsistency): lifecycle.py:47-49 rig_up hardcodes a2j→carla:main→looper:engine but the plan text requires "matching UNIT_START_ORDER" (reconcile.py:252: looper-engine 0, looper-mcp 1, a2j 2, carla-main 3, carla-child 4; used by do_load converge.py:191). Controller resolved toward the plan's stated requirement + existing single source of truth (no conflicting user decision) → fix dispatched (haiku): derive order from UNIT_START_ORDER, skip kinds without starters (looper-mcp legacy, carla-child from sessions) → looper:engine, a2j, carla:main; update pinned test; also Minor: remove unused json/Node imports in test_bridge2_lifecycle.py:1-7. Minor (final triage): spec §7 prose "tools contain no try/except" vs narrow typed try/except in rig_state helpers (allowed by global constraints). Tell user about the order change.
- Lint re-scan at 4197b1a (incl. Task 15 bridge/tools/*): 0 broad/bare except outside allowlist; 0 reasonless skips. ./dev lint should be clean for Task 17.
- Task 18 SAFETY: live smoke (tests/live/test_phase1_smoke.py) runs rig_up → rig_state → rig_down against REAL processes; fixture teardown ALWAYS calls rig_down (do_stop). Skips unless CARLA_MCP_LIVE=1. Task 18 IMPLEMENTER must only write files + run default (skipped) suite — NEVER set CARLA_MCP_LIVE=1. Controller runs ./dev live ONLY after explicit user go-ahead + confirming no rig/looper is running (unsaved loop audio is memory-only). Checking do_stop scope for adopted external instances.
- do_stop scope (converge.py): stops units in UNIT_STOP_ORDER via ops.stop_unit; for carla-main/looper-engine/a2j → units.stop_* → ProcessManager.stop(name) = no-op for units this bridge process did not spawn (adopted external instances SURVIVE; reported "still up after stop" → DEGRADED). carla-child → legacy SSE remove_node → DOES act on an external Carla. With graph=None uses DEFAULT_STOP_UNITS (no carla-child). So the smoke test (no session) cannot kill a running external rig, but would report DEGRADED; when no rig is running it spawns and then kills Carla/looper/a2j itself (grabs PipeWire ports while up). Still require explicit user go-ahead before ./dev live.
- Task 18 live test analysis (brief lines 26-66): fixture = Bridge.from_env + lifecycle tools, teardown always rig_down. Test: rig_up ok + "carla:main" in started (adoption counts as started); rig_state(detail="io") ok, carla version not down/unknown, some "loopers:" output port; rig_down report startswith "OK". → Meaningful ONLY with NO rig running: then it spawns Carla GUI (pw-jack python carla.py → opens a Carla window) + loopers --managed (opens looper GUI) + a2jmidid -e, verifies, stops them. With the user's rig already running it adopts, stops nothing (not ours), and FAILS on DEGRADED "still up after stop" without harming the rig. Live phase plan (controller, after Tasks 16-17 + final review): ask user for a window when the rig is idle and loops are saved/expendable; confirm nothing on 8088/8089 and no carla/loopers processes; then run Task 7 manual worker checks + ./dev live.
- Task 15 fix 1 (32efd3c, haiku): rig_up order derived from UNIT_START_ORDER inside build() (looper:engine, a2j, carla:main), test updated + tied to constant, unused json/Node imports removed; suite 787. Re-review dispatched (sonnet).
- Task 15: complete (commits 4c88736..32efd3c, review clean after 1 fix; rig_up order from UNIT_START_ORDER).
- Task 16 BASE: 32efd3c4c (dispatched sonnet).
- Pre-dispatch check: Task 18 — pyproject.toml [tool.pytest.ini_options] has testpaths + pythonpath only, NO markers key → adding markers=["live: ..."] is safe. testpaths includes carla_mcp/tests so tests/live is collected by default and must skip without CARLA_MCP_LIVE=1. tests/conftest.py exists (tests/live/conftest.py nests under it). Note for Task 18 review: pytestmark in a conftest.py does not mark tests (only in test modules). kb progress.md updated for Tasks 8-15 + fix rounds and published.
- kb publish confirmed: local 4f84bf8 == origin/main 4f84bf8, clean.
- Task 16 in-flight observations (from brief code, seen on disk): (a) session_save calls _wait_for_looper_project AFTER do_save — exists-only check, 10s, post-verify → redundant and weaker than decision-16 BridgeOps save_time wait (which runs BEFORE verify); per user's no-legacy/single-source preference it should be removed. (b) load_session_into raises ToolError("degraded", ...) — verify "degraded" is in ERROR_TYPES, else ToolError ctor raises → surfaces as internal. Raise both as context in the Task 16 review prompt (facts, not verdicts).
- Task 16 observation (b) RESOLVED: 'degraded' is in ERROR_TYPES (result.py:18); ToolError also maps unknown types to 'internal' rather than raising. Only (a) redundant _wait_for_looper_project remains as review context.
- Task 16 implemented (9debab8, sonnet; 5 tests, suite 792; fail-before 5 failures vs placeholder). Review dispatched (sonnet) → task-16-review.md, with context on decision-16 BridgeOps save wait vs brief's _wait_for_looper_project, and a path-traversal check on session names.
- Task 17 BASE: 9debab84c (dispatched haiku in parallel with read-only Task 16 review; implementer runs only ./dev lint, bash -n dev, ./dev help, pytest — never run/bridge/worker/live/mcp; stops before commit if lint reports findings). Any Task 16 review fix waits for Task 17 to commit.
- Final-review prep: compiled 20 Minor ledger lines (from MERGE_BASE anchor L49) into scratchpad/sdd/minors-for-final-review.md — hand this path to the final whole-branch reviewer (opus) along with review-package b135b4cef..HEAD. Rebuild it after Tasks 16-18 reviews to include any new Minors.
- Task 17 implemented (9a2e044, haiku; 4 lint-rule tests; ./dev lint 0 findings; commit touches only dev, linters/python_rules.py, tests/test_lint_rules.py). Implementer reported 'full suite 773 passed' — MISREPORT: controller re-ran uv run pytest -q at 9a2e044 → 796 passed (792 + 4). Tell reviewer the verified count.
- Task 17 review dispatched (sonnet) → task-17-review.md (range 9debab8..9a2e044; verified suite 796 given to reviewer).
- Task 18 BASE: 9a2e0444d (dispatched haiku; files-only + default skipped run; NEVER CARLA_MCP_LIVE; Step 3 skipped). Runs in parallel with read-only Task 16 + Task 17 reviews; any Task 16/17 fix waits for Task 18 commit.
- Task 16 review: spec ✅ / quality ❌. Important x2 (plan-mandated; controller resolves without new user decision — both follow approved decisions / add safety without contradicting plan text):
  (16a) sessions.py:149-157,171-173 _wait_for_looper_project duplicates decision-16 BridgeOps save wait with weaker exists-only check; adds 10s stall + misleading note when looper down → REMOVE (user no-legacy preference, decision 16 single source).
  (16b) sessions.py:137-139,166-168 session name unvalidated: absolute name discards session_dir, ".." traverses out → session_save(overwrite=True) can write outside the store; rig_state(compare=) same gap → add one validator (reject empty, absolute, path separators, "..", leading "."), ToolError("validation"), apply to session_save/session_load/load_session_into/rig_state compare.
  Minor (fold into fix): sessions.py:138 vs :167 is_dir() vs exists() inconsistency. Minor (final triage): session_save updates b.graph/session_name on re-read success regardless of do_save verdict. Fix dispatch WAITS for Task 18 commit.
- Task 16 fix scope (exact session-name → path joins): bridge/tools/sessions.py:29 load_session_into (also session_load path), sessions.py:58 session_save, bridge/tools/lifecycle.py:90 rig_state(compare=). Put the validator in one place (e.g. sessions.py session_path(b, name) or bridge/tools/__init__.py) and use it at all three; tests for '../x', '/abs', 'a/b', '', '.hidden' → validation, plus a valid name still works; remove _wait_for_looper_project + its constants/imports; tool_surface.json unchanged.
- Task 17 review: spec ✅ / quality ❌. Important x2 (plan-mandated; controller resolves — fixes make the rules do what the plan states, no contradiction): (17a) python_rules.py:84 allowlist uses bare endswith → "notbridge/result.py" false-exempt; (17b) python_rules.py:78 _BROAD regex misses tuple form "except (ValueError, Exception):". Fold-in Minors: python_rules.py:77 inline "except: pass" not flagged; regex false positives inside strings/docstrings. → Fix: AST-based handlers (ExceptHandler.type None / Name|Attribute|Tuple containing Exception|BaseException), segment-aware allowlist (rel == entry or endswith "/"+entry), skip-needs-reason via ast.Call on pytest.skip / pytest.mark.skip(if) without reason= keyword (keep brief semantics); output format + main() exit codes unchanged; add tests for each gap. Minor (final triage): dev worker) case cd source/frontend breaks relative-path args. Task 16 + Task 17 fixes → ONE fix agent, two commits, dispatched after Task 18 commits.
- Task 18 implemented (f1cd397, haiku; tests/live/{__init__,conftest,test_phase1_smoke}.py + pyproject markers; live test "1 skipped" with reason; suite 796 passed 1 skipped; CARLA_MCP_LIVE never set, Step 3 not run).
- AST pre-scan (controller) of bridge/backends/worker incl. tuple forms: 0 bare/broad excepts outside allowlist → AST-based linter stays at 0 findings.
- COMBINED FIX (Tasks 16+17) dispatched (sonnet), BASE f1cd39763, two commits (A: session name validator at sessions.py:29/58 + lifecycle.py:90, drop _wait_for_looper_project, is_dir consistency; B: AST-based python_rules, segment-aware allowlist, bare @pytest.mark.skip, syntax-error finding) → fix-16-17-report.md. Task 18 review dispatch next (read-only, parallel).
- Task 18 review dispatched (sonnet) → task-18-review.md (range 9a2e044..f1cd397), read-only, parallel with combined 16/17 fix agent.
- Task 18: complete (commits 9a2e044..f1cd397, review clean: spec ✅ / Approved, no issues). Reviewer verified: collection side-effect-free; skip precedes Bridge.from_env; -rs → 1 skipped; -m "not live" → 1 deselected; conftest pytestmark is dead code (harmless; test module carries its own mark); double rig_down harmless. ⚠️ resolved by live-phase precondition: with a rig already running, rig_down reports DEGRADED "still up after stop" → test false-fails (non-destructive) → live run requires NO rig running. Report inaccuracy (claimed skip after from_env) is report-only.
- ALL 18 TASKS IMPLEMENTED. Remaining: combined Task 16/17 fix (in flight) + its re-review → mark Tasks 16/17 complete → rebuild minors file → final whole-branch review (opus) → live phase (user go-ahead) → finishing-a-development-branch.
- COMBINED FIX DONE: Commit A a151831 (Task 16: session_path validator at load_session_into/session_save/rig_state compare, dropped _wait_for_looper_project, is_dir consistency); Commit B eef29e3 (Task 17: AST-based python_rules, segment-aware allowlist, tuple/inline excepts, bare @pytest.mark.skip, syntax-error finding). Suite 821 passed 1 skipped; ./dev lint 0 findings. Re-reviews dispatched in parallel (sonnet): task16fix range f1cd397..a151831, task17fix range a151831..eef29e3.
- Final-review prep REBUILT: scratchpad/sdd/minors-for-final-review.md now 23 entries (anchor = first merge-base line L49; earlier 3-entry rebuild used the wrong anchor). Rebuild again only if the Task 16/17 re-reviews add Minors.
- Live-phase checklist written: scratchpad/sdd/live-phase-checklist.md (preconditions incl. user go-ahead + pgrep/ss checks + no transport commands + kill only own PIDs; A: GUI worker ping/patchbay + busy-port path; B: headless worker CarlaChain_smoke on 8101; C: ./dev live).
- Task 16: complete (commits 32efd3c..a151831, review clean after 1 fix: session_path validator at all 3 name→path sites, redundant post-save wait removed; verified real session names pass and traversal/absolute/backslash variants rejected). Minor (final triage): sessions.py:48-49 NUL-byte name → raw ValueError at resolve() → surfaced as "internal" not "validation" (no escape).
- Task 17 fix re-review: spec ✅ (AST rewrite, except*, segment-aware allowlist scoped to no-broad-except, bare @pytest.mark.skip, no string FPs, syntax-error, ./dev lint 0) / quality ❌ on NEW Important: python_rules.py:102 findings.sort parses line from rendered "path:line" → ValueError for paths containing ":" (Windows-style). Minor: python_rules.py:70-75 nested tuple except ((ValueError, Exception), TypeError) missed. Follow-up fix dispatched (haiku, single file + tests, BASE eef29e30d).
- Final review template: .claude/skills/requesting-code-review/code-reviewer.md — point the final opus reviewer at this path (it reads it itself) plus review-package b135b4cef..HEAD, minors-for-final-review.md, spec + plan + decisions.md paths, global-constraints.md, and the ledger.
- Lint follow-up DONE (facd03b, haiku): check_text sorts by structured lineno (no re-parse of rendered path; colon paths no longer crash), _is_broad recurses nested tuples; lint tests 12 passed; ./dev lint 0 findings. Re-review dispatched (sonnet) → task-17-review.md '## Re-review 2' (range eef29e3..facd03b). Final whole-branch review waits for this verdict.
- Controller-verified at facd03b: uv run pytest -q (CARLA_MCP_LIVE unset) → 823 passed, 1 skipped (live); ./dev lint → python_rules: 0 finding(s). Use as test evidence for the final whole-branch review (reviewer need not re-run).
- Task 17: complete (commits 9debab8..facd03b, review clean after 2 fixes: AST-based rules, segment-aware allowlist, colon-safe sort, nested tuples).
- ALL 18 TASKS COMPLETE (b135b4cef..facd03bcc). Suite 823 passed / 1 skipped; ./dev lint 0. NEXT: final whole-branch review (opus) with package b135b4cef..facd03bcc + minors-for-final-review.md.
- FINAL WHOLE-BRANCH REVIEW dispatched (opus) → scratchpad/sdd/final-review.md; inputs: review-final-b135b4c-facd03b.diff (36 commits, ~201 KB), minors-for-final-review.md (26), template .claude/skills/requesting-code-review/code-reviewer.md, spec/plan/decisions, ledger, evidence 823 passed/1 skipped + lint 0. kb progress for Tasks 16-18 published (origin/main f2a31ee). After verdict: ONE fix agent for all fix-before-merge findings → re-verify → live phase (user go-ahead) → finishing-a-development-branch.
- FINAL REVIEW (opus) verdict: ready to merge after listed fixes, then live phase. 1 Critical + 8 Important + 13 Minor; constraints all pass; no test touches live ports/processes/sessions. Minor triage of 26 ledger entries: 4 fix-before-merge (L86/L111/L159 fold into I6; L135 real UnicodeDecodeError bug), 17 defer, 3 already-fixed, 2 n/a.
  FIX WAVE dispatched (opus, ONE agent, BASE facd03bcc, report final-fix-report.md), 10 commits: (1) I5 spawn stdin=DEVNULL+start_new_session, M5 spawn OSError→error string, I4 no second Carla when Carla runs without worker; (2) I1 RPC read limit 16 MiB + overrun→RpcError; (3) I7 per-Bridge asyncio.Lock, mutating tools fail fast when busy; (4) C1 rig_up(session) refused when core units already up (cold start only); (5) I2 rig_state/no-session observes core units, DEGRADED when down; (6) I6 stop_* "not started by this bridge; left running", rig_down clears graph only on OK; (7) L135 UnicodeDecodeError keep polling; (8) M2 FAILED save → ToolError degraded + rmdir empty dir it created; (9) I8 in-process session save/load roundtrip test with fake servers on ephemeral ports; (10) M6 snapshot pins param defaults, M7 drop tautological block.
  USER DECISION PENDING: I3 rig_reset_routing semantics (asked via AskUserQuestion: rewire-to-session [rec] / keep clean slate / drop for phase 1) — excluded from the wave; follow-up after answer.
  DEFERRED (record in decisions.md when publishing): I6 pid re-adoption after bridge restart; M1 autosave-before-destructive flag unused; M3 overwrite-in-place drops Carla state on degraded save (pre-existing); M8 GUI engine_stop no-op reply; M9 GUI title/modified flag on worker project load/save; M10 hand-edit notes placement/looper version/state in other tools; M11 duplicate GetState + SSE per observe; M12 commit prefixes (accept history); M13 /usr/bin/python3 fallback; plus 17 ledger Minors triaged defer.
- USER DECISION (2026-09-14) I3: rig_reset_routing → "Rewire to session": remove stray rig links + restore loaded session's desired edges; no unit start/stop, no looper/Carla load, loops untouched; refuse when no session loaded.
- FIX WAVE DONE (opus, facd03b..e5fbfa5, 10 commits): 2677842 I5/M5/I4; 0cdef45 I1; 358c581 I7; 1070bb9 C1; a1fc7ef I2; 5805bdf I6+L86/L111/L159; c8954cb L135; ace59f9 M2; db60ae7 I8 roundtrip; e5fbfa5 M6/M7. Suite 870 passed / 1 skipped; lint 0. Concerns: (a) C1 guard gap — Carla-without-worker + looper down not refused → follow-up; (b) a2j counts as core unit → no-session rig_state DEGRADED without a2jmidid (accepted: rig uses a2j for MIDI); (c) externally started a2j/any adopted unit → rig_down DEGRADED "left running" (live checklist precondition covers); (d) dry_run reset lock-free (read-only, fine); (e) pid re-adoption deferred → decisions.md; (f) review's 2nd M2 note (FAILED do_load in load_session_into) → follow-up.
- FOLLOW-UP dispatched (opus, BASE e5fbfa531, report rewire-report.md): C1 I3 rewire (new converge do_rewire, do_routing_reset untouched), C2 C1 guard reuses I4 detection, C3 FAILED load → ToolError degraded, graph unchanged. In parallel: fix-wave re-review (opus, read-only) on package review-finalfix-facd03b-e5fbfa5.diff — dispatch next turn once package verified.
- kb decisions 17 (I3 rewire, user) + 18 (final review fixed vs deferred) published (origin/main 884bf1a). FIX-WAVE RE-REVIEW dispatched (opus) → final-fix-review.md on review-finalfix-facd03b-e5fbfa5.diff (10 commits, ~97 KB), read-only, parallel with rewire follow-up implementer.
- REWIRE FOLLOW-UP DONE (opus, e5fbfa5..c6dd0f6): 2347a9d I3 rig_reset_routing rewires to loaded session via diff→plan→_apply_action (no unit start/port wait), refuses with no session, dry_run adds not_fixable_by_rewiring; 00875c6 C1 guard reuses carla_running_without_worker (renamed public); c6dd0f6 FAILED load → ToolError degraded, graph unchanged. Suite 885 passed / 1 skipped; lint 0. Implementer noted: an unmocked pgrep -f carla.py in C1 tests WOULD find the user's running Carla → now mocked. NOTE: a Carla instance IS running on this machine (user's rig) — live phase must wait. Re-review dispatched (opus) → rewire-review.md. Controller running full suite under a scratch isolation plugin (blocks real pgrep/pw-link/pw-jack/carla/loopers/a2jmidid spawns + connects to 8088/8089/8101/3001) to prove machine isolation.
- FIX WAVE RE-REVIEW (opus): all 12 items ✅, Approved, no Critical/Important. Reviewer disabled each fix and confirmed a covering test fails; I8 roundtrip 12 runs parallel all pass 1.7–2.4s. Minor: (a) C1 guard fails open when pw-link fails (looper ports look absent) → add 8088 reachable / looper.loopers() check (fold into isolation fix); (b) looperdooper JACK start prompt spins on EOF stdin (loopers_jack.rs:114-126, reached :845 even --managed) now that stdin=DEVNULL → fix belongs in looperdooper (treat EOF as "no") — tell user; (c) second-Carla detection only matches *carla.py or SSE port (system `carla` launcher without SSE unseen) — defer.
- ISOLATION CHECK (controller, scratch plugin blocking real rig binaries + live-port connects): 19 hits, 1 failure. (i) BRANCH test test_bridge2_ops.py::test_rig_handles_and_remove_node_use_default_timeout runs REAL read-only pw-link -o/-i/-o -l + pgrep -x a2jmidid (machine-dependent; must mock). (ii) PRE-EXISTING test_chain_builder.py::TestConnectSystemOutput::test_connect_last_plugin_to_system_output spawns real pw-link -i (and possibly a real connect in normal runs — investigating); fails under guard. (iii) PRE-EXISTING test_plugin_handle_tools.py x12 spawn /usr/lib/carla/carla-discovery-native (harmless scanner; guard matched "carla" prefix). No branch commits touched (ii)/(iii).
- ISOLATION HAZARD CONFIRMED: pre-existing test_chain_builder.py::TestConnectSystemOutput::test_connect_last_plugin_to_system_output → build_chain(connect_system_output=True) → ensure_carla_to_monitors() → real pw-link -i / -l and, if not already linked, REAL `pw-link Carla:audio-outN <monitor playback>` connect. Every full-suite run this session (~10) executed it on the user's live PipeWire graph (likely no-op if Carla outs were already wired to Scarlett). TELL USER. Controller ran read-only `pw-link -l` listing of Carla:audio-out1/2 links for the user.
- FREEZE: no pytest full-suite runs (controller or agents) without the isolation guard until the isolation fix lands.
- ISOLATION FIX dispatched (sonnet): autouse conftest guard (blocks pw-link/pw-jack/pw-metadata/pw-cli/pgrep/pkill/killall/a2jmidid/loopers/jack_connect/jack_disconnect + carla.py/carla_mcp.worker/carla_mcp.bridge argv + connects to 8088/8089/8101/3001/3002; exempt tests/live), guard test, mock ensure_carla_to_monitors in chain_builder test, mock pw_link/probes in test_bridge2_ops observe test; guard FIRST, then tests. Report isolation-fix-report.md. C1 fail-open fix (lifecycle.py) waits for rewire re-review verdict.
- Read-only live graph check (controller): Carla:audio-out1 → alsa_output.pci-0000_2b_00.3.analog-stereo:playback_FL, audio-out2 → :playback_FR (onboard analog). No links from Carla outs to pro-output (Scarlett-style) monitor ports → no trace that test_chain_builder's ensure_carla_to_monitors added links this session. Checking whether any input port even matches _MONITOR_OUTPUT_RE.
- DEFINITIVE (controller, read-only): _MONITOR_OUTPUT_RE = ^alsa_output\..*pro-output.*:playback_AUX\d+$ matches 0 of 44 live input ports (Scarlett not in pro-audio profile right now) → ensure_carla_to_monitors had no monitor ports → NO pw-link verify/connect ran this session; test_chain_builder only did a read-only pw-link -i. Hazard is real whenever the Scarlett is in pro-audio profile → isolation guard still required. Live Carla outs currently → onboard analog-stereo playback_FL/FR (user's own state; untouched).
- REWIRE FOLLOW-UP REVIEW (opus): 2347a9d ✅ / 00875c6 ✅ / c6dd0f6 ✅ — Approved (110 targeted tests; 7 mutation probes all caught). plan() emits only start_unit/wait_ports/disconnect/connect, so filtering is complete; disconnects limited to in_rig_port_space; connects via same expand_edges as do_load; do_routing_reset + legacy stdio path unchanged. REWIRE FOLLOW-UP: COMPLETE (e5fbfa5..c6dd0f6).
  PRE-LIVE FIX QUEUE (dispatch as ONE small fix after isolation-fix commits land): (P1) converge.py:304-305 do_rewire: if pw-link -o/-i fail (empty) while -l works, every link looks stray → all rig links cut; guard: never disconnect a link whose ports aren't in the observed port lists (and/or refuse when port lists are empty but links exist). (P2) lifecycle.py C1 guard fails open when pw-link fails (looper ports look absent) → also refuse when looper TCP 8088 reachable (tcp_reachable) or looper.loopers() non-empty. (P3) lifecycle.py:106-108 cold rig_up(session=) FAILED load error reply omits started units + starter notes → include them in ToolError notes.
  ⚠️ notes → deferrals/decision text: rewire cuts hand-wiring to loops created after last load/save (by design; agents should dry_run first — add to tool description); after a bridge restart there's no loop-preserving way back to a saved session's routing (phase-2 input with pid re-adoption).
- USER: "keep going" (not a live go-ahead). PRE-LIVE FIX (P1 do_rewire no disconnect on partial observation; P2 C1 guard also probes looper TCP 8088; P3 failed cold load keeps started units in notes + rig_reset_routing description advises dry_run first) dispatched (sonnet) in an ISOLATED WORKTREE, parallel with the isolation-guard agent on main checkout (no file overlap). Agent runs only targeted test files (not full suite / not test_chain_builder) since its base lacks the guard. After both land: cherry-pick worktree commits onto feat/bridge-owned-rig, run full suite with guard, review both.
- kb decision 19 (rewire limits: cuts unrecorded hand-wiring / dry_run first / no disconnect on partial observation; deferred loop-preserving restore after restart; unit-test live-rig isolation guard) published (origin/main 8c92e24). Memory saved: feedback_verify_test_isolation_before_suite.md.
- ISOLATION FIX DONE (sonnet): 929c867 autouse conftest guard (RigIsolationError(AssertionError); blocks rig binaries + carla.py/carla_mcp.worker/bridge argv + connects to 8088/8089/8101/3001/3002; tests/live exempt) + test_rig_isolation_guard.py; 952d185 mocks ensure_carla_to_monitors in chain_builder test + pw_link/probes in bridge ops observe test. Guard in place before any pytest ran. Full suite 890 passed / 1 skipped; scratch plugin run ISOLATION HITS 12 = only harmless carla-discovery-native. Note: source/frontend/__init__.py makes conftest import as frontend.carla_mcp.tests.conftest → guard test asserts AssertionError base class. FREEZE LIFTED. Review dispatch next (range c6dd0f6..952d185).
- Isolation fix REVIEW dispatched (sonnet) → isolation-review.md (range c6dd0f6..952d185; probes may add then delete temp test files under tests/, must leave git status clean). NOTE: git status shows untracked .claude/worktrees/ (pre-live agent's isolated worktree) — never stage it; consider adding .claude/worktrees/ to .gitignore at finish time (not mid-run).
- ISOLATION FIX REVIEW (sonnet): correctness ✅ (blocks list/shell=True/path-prefixed rig binaries, IPv4/IPv6 + asyncio.open_connection to rig ports; monkeypatch restore; tests/live exempt; module-level mocks override), quality Approved, no Critical/Important. Reviewer's temp probe files deleted, git status clean. ISOLATION FIX: COMPLETE (c6dd0f6..952d185).
  Minor/⚠️ (defer, no current exposure — grep confirms unused in production): guard doesn't wrap os.system / os.spawn* / os.exec* / asyncio.create_subprocess_exec|_shell, and ["sh","-c","pw-link ..."] / ["env","pw-link",...] bypass basename check; xdist behaviour untested (not installed).
- BASELINE (controller) at 952d185 with permanent conftest isolation guard: uv run pytest -q (CARLA_MCP_LIVE unset) → 890 passed, 1 skipped; ./dev lint → 0 findings. Next: cherry-pick pre-live worktree commits (P1-P3) on top, re-run suite + lint, review.
- PRE-LIVE FIX cherry-picked onto feat/bridge-owned-rig (clean): c342aad→201bd32 P1 do_rewire no disconnect when link ports absent / port lists empty; 5efc671→de50bab P2 C1 guard also probes looper TCP port; 0d5337a→76dd1cc P3 failed cold load notes include started units + rig_reset_routing advises dry_run. Guarded suite 894 passed / 1 skipped; lint 0. Agent worktree removed after verifying all 3 picks. Review dispatch next (range 952d185..76dd1cc).
- CORRECTION: agent worktree NOT removed — git refused ("working trees containing submodules cannot be moved or removed"); branch worktree-agent-abf5e689e3d1f6873 NOT deleted (chain stopped). All 3 commits verified present on feat/bridge-owned-rig by subject. Cleanup deferred to finishing step after verifying the worktree's kb submodule has no local changes/unpushed commits (then deinit/remove --force or rm + git worktree prune). Pre-live fix REVIEW dispatched (sonnet) → prelive-review.md.
- Agent worktree cleaned up: worktree kb submodule verified clean + HEAD 8c92e24 on origin/main + no unique top-level changes; submodule deinit, git worktree remove --force, branch worktree-agent-abf5e689e3d1f6873 deleted. User's .worktrees/spectrum-driven-eq untouched.
- PRE-LIVE FIX REVIEW (sonnet): 201bd32 ✅ / de50bab ✅ / 76dd1cc ✅ — Approved, no issues; each fix mutation-killed; lock release verified; snapshot has no description keys. PRE-LIVE FIX: COMPLETE (952d185..76dd1cc). ⚠️: legacy do_routing_reset lacks port-list guard (out of scope, legacy stdio only); none of the 3 exercised against real pw-link/TCP → live spot check.
- CODE COMPLETE for phase 1 at 76dd1ccb0: suite 894 passed / 1 skipped (guarded), lint 0. Remaining: live phase (needs explicit user go-ahead: rig idle, loops saved/expendable; checklist scratchpad/sdd/live-phase-checklist.md A-E) → finishing-a-development-branch (push blocked on spec review PR #3).
- USER (2026-09-14): live checks "Now — rig is idle".
- LIVE PRECONDITION FAILED → INCIDENT: orphan rig processes spawned by THIS SESSION, not the user: loopers --managed --remote-json-port 2 (pid 196261, started 18:02:16), carla.py x2 (pids 196337 18:02:16, 196674 18:02:37); env CLAUDECODE=1, CARLA_RPC_PORT=1, CARLA_MCP_PORT=1 (= Bridge.for_tests closed ports 1/2); own session/pgid 196253 (start_new_session=True from I5 fix), ppid 1; listening only on Carla OSC 22752/22753; present as Carla + loopers JACK clients in the user's PipeWire graph for ~1h40m. Timing = final-review fix wave, BEFORE the conftest isolation guard existed. a2jmidid pid 62251 (since Sep 8) is the user's. Earlier assumption "a Carla is running = user's rig" was WRONG — it was these orphans (the Carla→analog-stereo links seen earlier likely theirs). NOT killing anything without user permission. Investigating which test/probe spawned them; the current guard blocks pw-jack/carla.py/loopers argv so the committed suite can no longer do this.
- INCIDENT IMPACT (read-only pw-link -l): orphan loopers wired into the user's real I/O — loopers:loop0_in_l/r ← alsa_input.pci-0000_2b_00.3.analog-stereo:capture_FL; loopers:loopers_midi_in ← bluez_midi.server:out (user's BT MIDI controller), Midi-Bridge Midi Through, a2j Midi Through; loopers:metronome_out_l/r → analog-stereo playback_FL/FR; Carla:audio-out1/2 (x2 instances) → analog-stereo playback_FL/FR. Spawned 18:02, before fix-wave commits 18:29-18:38 → most likely the final whole-branch reviewer's probes (it ran rig_up/units probes; reviewer was told not to spawn rig processes). Ad-hoc `uv run python` probes bypass the conftest guard. Asked user (AskUserQuestion) to stop orphans (pgid 196253) + how to treat their a2jmidid (pid 62251) for live checks.
- USER: stop orphans (pgid 196253) = yes; leave a2jmidid (62251) running for live checks = yes (treat 'a2j left running' in rig_down as expected). Controller verified pgid members+cmdlines, SIGTERM'd pgid 196253 (first attempt aborted pre-signal by a zsh word-splitting bug in the check script; re-run under bash).
- LIVE A (controller, user-approved): GUI RPC worker on 8089 — ping→pong, engine_info running 48k/256 client Carla, patchbay_refresh ok, patchbay_list 4 groups ✅; busy-port path (8089 held) → GUI stays up ✅; but "RPC worker ..." stderr lines not found in captured Carla logs (investigating). Cleanup clean.
- LIVE B: headless worker CarlaChain_smoke on 8101 — up 2s, 34 JACK ports, engine_info running, engine_stop → process exited 0 on its own, ports removed ✅. Cleanup clean.
- LIVE C (./dev live) running; a2jmidid (user's) intentionally left running → rig_down expected DEGRADED "left running" → smoke's final OK assertion expected to fail on that alone.
- LIVE A note: missing "RPC worker ..." stderr lines explained — carla_host.py:3605-3606 redirects sys.stdout/stderr to CarlaPrint (Carla's log window), so they never reach the captured log file. Not a defect.
- LIVE C (./dev live, 8.2s): rig_up ok + carla:main started; rig_state ok, carla version up, loopers ports in io; rig_down stopped carla:main/looper:mcp/looper:engine. Only failure = expected: "stop a2j: not started by this bridge; left running" + "still up after stop: a2j" (user's a2jmidid kept by user choice) → DEGRADED → smoke's OK assertion failed on that alone ✅ (expected). No leftovers; a2jmidid untouched.
- System /usr/bin/python3 has fastmcp 2.10.1 + mcp → in-GUI legacy MCP server imports fine (needed for export/import_rig_state in phase 1).
- LIVE D (session save/load vs scratch RIG_SESSION_DIR under scratchpad/sdd/live/rig-sessions-*) running.
- LIVE D attempt 1 aborted by a controller driver bug (step(label, name, **kw) collided with session_save name= kwarg → TypeError) after rig_up ok (2.2s) + rig_state OK (no session loaded); finally rig_down ran: stopped carla:main/looper:engine, a2j left running (expected). No leftovers, rig ports free. Re-running with fixed helper and a fresh scratch RIG_SESSION_DIR.
- LIVE D attempt 2 ✅ (scratch RIG_SESSION_DIR scratchpad/sdd/live/rig-sessions-sdjY): rig_up ok 2.7s (state OK no session); session_save OK 0.4s → carla_project.carxp 2278B, rig_session.json 4543B, looper/project.loopers 1236B (decision-16 save wait satisfied); session_list ok; rig_reset_routing dry_run ok; session_load OK 0.6s (state OK); rig_state after load OK; rig_down stopped carla:main/looper:engine, only issue a2j left running (expected). No leftovers; a2jmidid untouched; rig ports free.
- LIVE PHASE COMPLETE: A ✅ GUI worker (8089) + busy-port survives; B ✅ headless worker engine_info/engine_stop exit 0; C ✅ ./dev live (sole failure = expected user-a2j left running); D ✅ session save/load roundtrip. Not covered: heavy-session durations vs 120s limits (empty rig used); Claude Code MCP timeout for multi-minute calls; phase-2 patchbay_refresh canvas duplication. NEXT: finishing-a-development-branch (push blocked on spec review PR #3).
- FINISHING (skill): tests 894 passed / 1 skipped, lint 0 at 7a5fa9e (ledger committed). Normal repo, feat/bridge-owned-rig 56 commits ahead of main, not on origin. USER CHOSE: push + PR. Spec review PR #3 (roboloops-harness) still OPEN, reviewDecision empty → pre-push hook would block. `git push -u origin feat/bridge-owned-rig` was DENIED by the Claude Code auto-mode classifier (not attempted around). PR body drafted at scratchpad/sdd/pr-body.md. WAITING ON USER: approve + `rcorn review merge bridge-owned-rig-mcp-single-server-thin-carla-worker-name-ad`, and allow/run the push; then `gh pr create --base main --head feat/bridge-owned-rig --body-file scratchpad/sdd/pr-body.md`.
- REVIEW of 033e8d482 (2026-09-18, read-only agent): Approved, no Critical. Important: BridgeOps.observe lost never-raises shielding (bare gather) → FIXED (return_exceptions, failures read as empty/down, new test). Also fixed: units._wait sleep clamped to remaining deadline; rig_up awaits an awaitable returned by a sync starter. Deferred Minors: spawned-but-not-ready unit in neither started/already_up; a2j has no readiness wait; to_thread + cancellation race on ProcessManager; observe fans out N+4 concurrent pw-link calls (probes could reuse the snapshot). Guarded suite 899 passed / 1 skipped; lint 0.
- PUSHED feat/bridge-owned-rig to origin (2026-09-18, user: "do everything"); PR opened: https://github.com/mnbiehl/Carla/pull/1. Spec review PR #3 still OPEN/unapproved — `rcorn review merge` denied to the agent by the auto-mode classifier (merge without review); user must approve + merge.
